public String divide(Integer a, Integer b){
    //code here
    try {
        Integer c = a / b;
        String answer = String.valueOf(c);
        return answer;
    }
    catch ( NullPointerException npe) {
        String exceptionMessage = npe.getMessage();
        return exceptionMessage;
    }
}