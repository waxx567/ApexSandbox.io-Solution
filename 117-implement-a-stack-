public class Stack {
    List<Object> stackList = new List<Object>();
    
    public void push(Object obj) {
        //implement push
        stackList.add(obj);
    }
    
    public Object pop() {
        //implement pop
        Object popped = stackList.remove(stackList.size()-1);
        return popped;
    }
    
    public Integer size() {
        //implement size

    }
    
    public Object peek(){
        //implement peek
        if (!stackList.isEmpty) {
            stackList[-1];
        }
    }
    
    public Boolean isEmpty() {
        //implement isEmpty
        if (stack.size() == 0) {
            isEmpty == true;
        }
    }
}