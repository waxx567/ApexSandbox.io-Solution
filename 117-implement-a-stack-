public class Stack {
    List<Object> stackList = new List<Object>();
    
    public void push(Object obj) {
        //implement push
        stackList.add(obj);
    }
    
    public Object pop() {
        //implement pop
        return stackList.remove(-1);
    }
    
    public Integer size() {
        //implement size
        Integer listSize = 0;
        for (Object o : stackList) {
            listSize =+ 1;
        }
        return listSize;
    }
    
    public Object peek(){
        //implement peek
        if (stackList.size() == 0) {
            return stackList.get(-1);
        } else {
            return null;
        }
    }
    
    public Boolean isEmpty() {
        //implement isEmpty
        Boolean empty = false;
        if (stack.size() == 0) {
            empty = true;
        }
        return empty;
    }
}