public Integer sumToN(Integer n) {
    
    List<Integer> numList = new List<Integer>();

    for (Integer i = 1; i < n; i++) {
        numList.add(i);
    }

    Integer answer = 0;

    for (Integer AddInt : numList) {
        answer += AddInt;
    }

    answer += n;

    return answer;
}