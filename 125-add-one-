public void plusOne(List<Integer> numbers) {
    Integer count = numbers.size() - 1;
    if (numbers.get(count) == 9) {
        numbers.add(count, 0);
        count -= 2;
        numbers.add(count, numbers.get(count)+1);
    } else {
        numbers.add(count, numbers.get(count)+1);
    }
}