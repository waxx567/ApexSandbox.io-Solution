public Map<String, List<Case>> casesByType(List<Case> cases) {
    
    Map<String, List<Case>> typedCases = new Map<String, List<Case>>();

    // Use the following lines to add an element 
    // when a key does not currently exist:
    // myMap.put(myCase.Type, new List { myCase }).
    // when a key already exists: 
    // myMap.get(myCase.Type).add(myCase).

    return typedCases;
}