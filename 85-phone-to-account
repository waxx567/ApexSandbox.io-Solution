public Map<String, Account> phoneToAccount(List<Account> accounts) {
    
    Map<String, Account> phoneMap = new Map<String, Account>();

    for (Account acc : accounts) {
        if (accounts.Phone != null) {
            phoneMap.put(accounts.Phone, Account);
        } else if (accounts.Phone != null) {
            return new Map<String, Account>();
        }
    }
}