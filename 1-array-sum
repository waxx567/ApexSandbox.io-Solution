public static Integer arraySum(List<Integer> numbers) {
    // Null check
    if (numbers == null) {
        return null;
    } else {
        // Loop over the numbers input
        Integer sumTotal = 0;
        for (Integer i = 0; i < numbers.size(); i++) {
            sumTotal += i;
        }
        return sumTotal;
    }
}