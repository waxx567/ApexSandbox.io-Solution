public static Integer arraySum(List<Integer> numbers) {
    // Null check
    if (numbers == null) {
        return null;
    } else {
        Integer arrayTotal = 0;
        // Loop over the numbers input
        for (Integer i = 0; i < numbers.size(); i++) {
            // Add each number to the total
            arrayTotal += numbers[i];
        }
        return arrayTotal;
    }
}